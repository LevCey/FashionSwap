import React from 'react';

// Simplified provider for demo - wallet integration to be added later
export const StarknetProvider: React.FC<{ children: React.ReactNode }> = ({ children }) => {
  return <>{children}</>;
};
